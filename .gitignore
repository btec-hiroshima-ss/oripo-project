node_modules/
.next/
.env*.local
.env

# macOS
.DS_Store

# バックアップ（ファイルは管理外、ディレクトリのみ管理）
backups/*.dump.gz
backups/logs/*.log.gz

# アプリログ（ファイルは管理外、ディレクトリのみ管理）
logs/*.log

# Claude ローカル設定
.claude/settings.local.json

# ローカル参照用ドキュメント置き場（git管理外）
/shareDir/*
!/shareDir/ここのフォルダ内はclaudeコンテナにマウント(共有)されます.txt
!/shareDir/構築方法.txt
